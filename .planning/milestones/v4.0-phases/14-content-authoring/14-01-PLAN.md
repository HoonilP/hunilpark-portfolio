---
phase: 14-content-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/ko.json
  - messages/en.json
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - "Joshua project has 2 challenges with IPC architecture and KoGPT-2 AI pipeline topics"
    - "DY CMS project has 2 challenges with architecture decision and accounting automation topics"
    - "Retail Analysis project has 2 challenges with VanillaJS rationale and YOLO pipeline topics"
    - "All 6 outcome fields contain quantitative figures (numbers, percentages, before/after)"
    - "All 6 alternatives fields explicitly reject at least one approach with rationale"
    - "Joshua and Retail Analysis each have at least one AI-specific challenge"
    - "ko.json and en.json challenge keys match exactly for joshua, dyCms, retailAnalysis"
  artifacts:
    - path: "messages/ko.json"
      provides: "Korean challenge content for joshua, dyCms, retailAnalysis"
      contains: "challenge2"
    - path: "messages/en.json"
      provides: "English challenge content for joshua, dyCms, retailAnalysis"
      contains: "challenge2"
  key_links:
    - from: "messages/ko.json"
      to: "messages/en.json"
      via: "identical challenge key structure"
      pattern: "challenges\\.challenge[12]\\.(title|context|alternatives|decision|implementation|outcome)"
---

<objective>
Author bilingual engineering challenge content for Joshua, DY CMS, and Retail Analysis — the three career/company projects with the strongest quantitative data from resume and portfolio PDFs.

Purpose: Replace stub placeholder challenges with deeply researched engineering narratives that demonstrate problem-solving ability to recruiters. Each challenge includes explicitly rejected alternatives and quantitative outcomes grounded in source documents (CONT-06).

Output: 6 challenges (2 per project) in both ko.json and en.json, with key parity validated.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-content-authoring/14-RESEARCH.md

Source documents (MUST read before writing any content):
@자료/박훈일 이력서.pdf
@자료/박훈일 포트폴리오.pdf

Existing i18n files (read to understand current structure):
@messages/ko.json
@messages/en.json

Component contracts (understand what fields are rendered):
@src/components/projects/ProjectContent.tsx
@src/components/projects/ChallengeSection.tsx

<interfaces>
<!-- CHALLENGE_KEYS = ['challenge1', 'challenge2', 'challenge3'] from ProjectContent.tsx -->
<!-- ChallengeSection renders: title, context, alternatives, decision, implementation, outcome -->
<!-- Optional: code + codeLang fields (rendered via CodeBlock if t.has() returns true) -->

Per-challenge JSON schema:
```json
{
  "title": "string — concise challenge name",
  "context": "string — 2-4 sentences, problem definition and why it mattered",
  "alternatives": "string — 2-4 sentences, approaches considered and WHY each was rejected",
  "decision": "string — 2-3 sentences, why chosen approach was selected (tech stack rationale)",
  "implementation": "string — 2-4 sentences, how it was implemented concretely",
  "outcome": "string — 1-3 sentences, MUST include quantitative figure",
  "code": "string (OPTIONAL) — code snippet if it illustrates the engineering decision",
  "codeLang": "string (OPTIONAL) — language identifier for syntax highlighting"
}
```
</interfaces>

<verified_facts>
<!-- CRITICAL: All content MUST come from these verified facts. No fabrication (CONT-06). -->

Joshua AI Agent:
- KoGPT-2 Fine-tuning, keyword-based prompt design/optimization for copywriting auto-generation
- Reduced unnecessary token generation, optimized inference speed for real-time response pipeline
- Electron + Angular desktop app — Windows/macOS cross-platform
- Stripe payment integration — paid AI service access and permission control
- GPU bare-metal server setup, model inference request handling
- Main process (API) ↔ Renderer process (UI) IPC separation
- RxJS-based async streaming — real-time AI response rendering
- Result: SI project delivered successfully, Windows/macOS cross-platform launch
- Team: 3 (2 designers, 1 planner/developer)
- Tech: Electron, Angular, FastAPI, PostgreSQL

DY Microfinance CMS:
- Previous: Excel spreadsheets + manual work → monthly closing took dozens of hours, frequent data integrity errors
- Next.js App Router (frontend) + NestJS (backend) + PostgreSQL separated architecture
- Server Actions for form handling, RBAC frontend routing level (3 roles: loan consultant/accountant/admin)
- BS (balance sheet) + PL (income statement) auto-generation interface
- Result: ~90% accounting process automation, monthly closing time reduced from dozens of hours to a few hours
- Solo full-stack development (1 person)
- Tech: NestJS, Next.js

Retail Store Analysis:
- YOLO-based object recognition tuned on store CCTV footage for customer identification/tracking
- VanillaJS chosen: verify complex real-time data visualization without React/Vue + bundle size/performance optimization
- Canvas API + SVG heatmap, WebSocket real-time data reception
- Observer pattern state management, requestAnimationFrame + debouncing render optimization
- Bare-metal Linux server setup, on-premise analysis (no cloud)
- Extracted: customer movement paths, dwell time, congestion metrics
- Team: 3
- Tech: Pytorch, VanillaJS
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Joshua and DY CMS challenges (ko + en)</name>
  <files>messages/ko.json, messages/en.json</files>
  <action>
Replace the stub challenge1 content and add challenge2 for both Joshua and DY CMS in ko.json and en.json.

**Joshua (2 challenges):**

challenge1: "Electron IPC 아키텍처 설계" / "Electron IPC Architecture Design"
- context: Electron의 Main↔Renderer 프로세스 분리 환경에서 AI 추론 요청, Stripe 결제 이벤트, UI 상태를 안전하게 동기화해야 하는 문제. 데스크톱 앱이라 브라우저처럼 단순 fetch로 처리할 수 없고, IPC 채널 설계가 전체 앱 안정성을 좌우.
- alternatives: (1) Renderer에서 직접 외부 API 호출 — 보안 취약(API 키 노출), CORS 문제로 기각. (2) 단일 프로세스 Node.js 앱(CLI 형태) — 디자이너와 협업한 UI/UX 반영 불가, SI 납품 요건(GUI) 미충족으로 기각.
- decision: Electron + Angular 선택. Main 프로세스에서 모든 외부 통신(FastAPI, Stripe)을 담당하고, Renderer는 UI만 처리하는 엄격한 IPC 분리. Angular의 DI 시스템과 RxJS 스트리밍이 AI 응답 실시간 렌더링에 적합했고, 클라이언트가 Windows/macOS 크로스플랫폼을 요구.
- implementation: ipcMain에서 FastAPI 서버로 추론 요청을 중계하고, ipcRenderer는 RxJS Observable로 AI 응답을 스트리밍 수신. Stripe webhook 이벤트도 Main 프로세스에서 처리하여 결제 상태에 따른 기능 잠금/해제를 UI에 전파.
- outcome: SI 프로젝트 정상 납품 완료, Windows/macOS 크로스플랫폼 데스크톱 앱 출시. 3인 팀(디자이너 2, 기획·개발 1)으로 전체 개발 주기를 완수.

challenge2: "KoGPT-2 추론 파이프라인 최적화" / "KoGPT-2 Inference Pipeline Optimization" (AI challenge — CONT-04)
- context: 카피라이팅 문구 자동 생성을 위해 KoGPT-2를 Fine-tuning했으나, 기본 생성 설정에서 불필요한 토큰이 과다 생성되어 응답 지연과 품질 저하가 발생. 실사용 환경에서 빠른 응답이 필수적인 SI 납품 요건.
- alternatives: (1) GPT-3/ChatGPT API 사용 — 2022년 당시 한국어 카피라이팅 품질 불안정, 외부 API 의존으로 오프라인 환경 지원 불가, 비용 구조가 SI 납품 모델에 부적합하여 기각. (2) 규칙 기반 템플릿 생성 — 키워드 조합 다양성이 부족하고 마케터가 기대하는 창의적 문구 생성 한계로 기각.
- decision: KoGPT-2 Fine-tuning + GPU 베어메탈 직접 구성. 키워드 기반 입력 구조에 맞춰 프롬프트를 설계하고, 불필요한 토큰 생성을 줄이는 생성 파라미터 최적화 적용. 내부 GPU 서버를 클라우드처럼 활용하여 추론 요청을 안정적으로 처리.
- implementation: 키워드를 중심으로 한 프롬프트 구조를 설계하여 모델이 관련 문맥에 집중하도록 유도. top-k, temperature 등 생성 파라미터를 실험적으로 조정하여 품질과 속도의 균형점을 확보. FastAPI 서버에서 추론 엔드포인트를 제공하고 Electron Main 프로세스가 이를 호출.
- outcome: 프롬프트 구조 개선으로 불필요한 토큰 생성을 줄이고 추론 속도를 최적화하여, 실사용 환경에서 빠른 응답이 가능한 생성 파이프라인을 구현. SI 프로젝트 정상 납품 완료.

**DY CMS (2 challenges):**

challenge1: "프론트엔드-백엔드 분리형 아키텍처 설계" / "Frontend-Backend Separated Architecture Design"
- context: 기존 DY Microfinance의 업무 환경은 Excel 스프레드시트와 수작업에 의존하여 월말 결산에 수십 시간이 소요되고, 데이터 정합성 오류가 빈번. 이를 체계적인 시스템으로 전환해야 했으나, 솔로 풀스택 개발(1인)이라는 제약 하에서 유지보수성과 확장성을 확보해야 했음.
- alternatives: (1) 모놀리식 풀스택 프레임워크(예: Django) — 프론트엔드 자유도가 낮고 대시보드 UI를 현대적 SPA로 구현하기 어려워 기각. (2) Firebase/Supabase BaaS — 마이크로파이낸스 특유의 복잡한 회계 계산 로직을 서버리스로 구현하기 어렵고, 미얀마 현지 네트워크 환경에서 실시간 DB 의존이 부적합하여 기각.
- decision: Next.js App Router(프론트엔드) + NestJS(백엔드) + PostgreSQL 분리형 아키텍처 선택. Next.js의 Server Actions로 폼 처리를 간소화하고, NestJS의 모듈 시스템으로 회계 로직을 도메인별로 분리. 1인 개발이지만 프론트/백 경계를 명확히 하여 이후 ERP 확장 시 팀 분업이 가능한 구조를 확보.
- implementation: NestJS에서 대출·상환 거래 CRUD와 BS/PL 자동 계산 API를 구현하고, Next.js 대시보드에서 RBAC 기반 라우팅(대출 상담사/회계 담당자/관리자 3역할)을 적용. Server Actions로 폼 제출 시 서버 사이드 검증 후 NestJS API를 호출하는 구조.
- outcome: 전체 회계 프로세스 약 90% 자동화 달성. 월말 결산 시간을 기존 수십 시간에서 수 시간으로 단축. ERP 확장 업데이트 협의가 진행될 만큼 시스템 안정성 확보.

challenge2: "RBAC 기반 역할별 접근 제어 구현" / "RBAC Role-Based Access Control Implementation"
- context: 마이크로파이낸스 운영에서 대출 상담사, 회계 담당자, 관리자의 업무 범위가 명확히 다름. 상담사가 회계 데이터를 수정하거나 관리자 이외의 사용자가 시스템 설정을 변경하면 재무 데이터 정합성이 무너질 위험. 역할별 접근 제어가 비즈니스 요건의 핵심.
- alternatives: (1) 페이지 단위 단순 라우트 가드만 적용 — API 레벨 검증 없이 프론트엔드만 가드하면 직접 API 호출로 우회 가능, 기각. (2) 외부 인증 서비스(Auth0, Clerk) — 미얀마 현지 인터넷 환경에서 외부 서비스 의존도를 낮춰야 했고, 소규모 사용자(수십 명)에 과한 비용 구조로 기각.
- decision: JWT 기반 자체 RBAC 구현. NestJS Guard에서 역할 검증, Next.js 미들웨어에서 라우트 레벨 접근 제어를 이중으로 적용. 실제 마이크로파이낸스 회계·운영 경험을 바탕으로 역할별 권한 매트릭스를 직접 정의.
- implementation: NestJS에서 JWT 토큰에 role 클레임을 포함하고, 커스텀 Guard 데코레이터로 API 엔드포인트별 역할 검증. Next.js 프론트엔드에서는 미들웨어로 라우트 접근을 필터링하고, 역할에 따라 다른 대시보드 레이아웃을 렌더링. 3역할(상담사/회계/관리자) 각각의 메뉴와 데이터 조회 범위를 분리.
- outcome: 3역할 기반 접근 제어를 프론트엔드 라우팅과 백엔드 API 양쪽에서 이중 검증하여, 기존 수기·분산 관리되던 운영 정보의 보안성과 데이터 정합성을 확보. 약 90% 회계 자동화의 기반이 되는 안전한 다중 사용자 환경 구축.

**Writing guidelines:**
- Write ko FIRST, then immediately write matching en for the same project
- English tone: professional technical English, first-person past tense ("I designed X to solve Y, which reduced Z by N%")
- Korean tone: match existing ko.json overview style — professional, specific, past tense narrative
- Keep `challenges.title` as "엔지니어링 챌린지" (ko) / "Engineering Challenges" (en) — unchanged from stub
- Joshua challenge1 keeps its existing `code` and `codeLang` fields — update the code snippet to show a meaningful IPC pattern (Electron ipcMain/ipcRenderer example, ~10-15 lines)
- Remove Joshua stub code/codeLang if not illustrative. Add code field to challenge2 if a KoGPT-2 prompt structure snippet adds value.

**JSON safety:**
- After editing each project pair, run: `python3 -c "import json; json.load(open('messages/ko.json')); json.load(open('messages/en.json')); print('JSON OK')"`
- Use careful attention to commas, quotes, and brace nesting in large JSON edits
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && python3 -c "
import json
with open('messages/ko.json') as f: ko = json.load(f)
with open('messages/en.json') as f: en = json.load(f)
for p in ['joshua','dyCms']:
    ko_ch = ko['ProjectDetail'][p]['challenges']
    en_ch = en['ProjectDetail'][p]['challenges']
    assert 'challenge1' in ko_ch and 'challenge2' in ko_ch, f'{p}: missing challenge in ko'
    assert 'challenge1' in en_ch and 'challenge2' in en_ch, f'{p}: missing challenge in en'
    assert set(ko_ch.keys()) == set(en_ch.keys()), f'{p}: key mismatch'
    for ck in ['challenge1','challenge2']:
        assert 'Phase 14' not in ko_ch[ck].get('context',''), f'{p}/{ck}: still has stub text in ko'
        assert 'Phase 14' not in en_ch[ck].get('context',''), f'{p}/{ck}: still has stub text in en'
        outcome = ko_ch[ck].get('outcome','')
        import re
        assert re.search(r'[0-9%]', outcome), f'{p}/{ck}: outcome missing quantitative figure'
print('PASS: joshua, dyCms challenges validated')
"</automated>
  </verify>
  <done>
Joshua has 2 challenges (IPC architecture + KoGPT-2 AI pipeline) in both locales. DY CMS has 2 challenges (architecture + RBAC) in both locales. Retail Analysis ready for Task 2. All outcome fields contain numbers/percentages. All alternatives explicitly reject at least one approach. No stub placeholder text remains. ko/en keys match.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Retail Analysis challenges (ko + en) and validate all 3 projects</name>
  <files>messages/ko.json, messages/en.json</files>
  <action>
Replace the stub challenge1 content and add challenge2 for Retail Analysis in ko.json and en.json.

**Retail Analysis (2 challenges):**

challenge1: "VanillaJS 기반 실시간 데이터 시각화 아키텍처" / "VanillaJS Real-Time Data Visualization Architecture"
- context: 매장 CCTV 영상에서 YOLO로 추출한 고객 동선·체류 시간·혼잡도 데이터를 실시간으로 대시보드에 시각화해야 했음. 데이터 갱신 주기가 짧고(초 단위), Canvas/SVG 히트맵과 WebSocket 스트리밍을 동시에 처리해야 하는 고성능 렌더링 요건.
- alternatives: (1) React — 가상 DOM diffing 오버헤드가 초 단위 실시간 Canvas 업데이트에 불필요한 재렌더링을 유발하고, YOLO 분석 결과를 직접 Canvas에 그리는 작업에서 프레임워크 추상화가 오히려 방해. 번들 사이즈도 On-premise 환경에서 불필요하게 증가하여 기각. (2) D3.js 단독 — 히트맵 시각화에는 적합하나 WebSocket 실시간 데이터 수신과 상태 관리까지 D3로 처리하면 코드 복잡도가 급증하여 기각.
- decision: VanillaJS 선택. Canvas API + SVG 히트맵으로 시각화를 직접 구현하고, Observer 패턴으로 상태 관리, WebSocket으로 실시간 데이터 수신. 프레임워크 없이 requestAnimationFrame + Debouncing으로 렌더링 최적화. 번들 의존성을 최소화하여 On-premise 매장 서버 환경에서 안정적으로 동작.
- implementation: WebSocket 연결로 YOLO 분석 서버에서 실시간 고객 좌표/행동 데이터를 수신. Observer 패턴으로 데이터 변경을 구독하는 뷰 컴포넌트들에 전파. Canvas에 고객 이동 경로를 그리고 SVG 히트맵으로 체류 시간을 색상 매핑하여 시각화. requestAnimationFrame으로 60fps 타겟 렌더링, Debouncing으로 과도한 WebSocket 이벤트 처리를 제어.
- outcome: 매장 내 고객 이동 경로, 체류 시간, 혼잡도 등 행동 지표를 실시간으로 시각화하는 대시보드를 구현하여, 관계자들이 매장 동선 최적화 및 상품 배치 전략을 데이터 기반으로 수립할 수 있는 환경을 제공.

challenge2: "YOLO 기반 On-Premise 영상 분석 파이프라인 구축" / "YOLO-Based On-Premise Video Analysis Pipeline" (AI challenge — CONT-04)
- context: 말레이시아 리테일 매장의 기존 CCTV 영상에서 고객을 식별·Tracking하여 행동 데이터를 추출해야 했음. 매장 환경(조명, 카메라 각도, 고객 밀집도)이 일반 YOLO 학습 데이터와 달라 기본 모델로는 인식률이 부족. 클라우드 의존 없이 현장 설치형 분석이 요구됨.
- alternatives: (1) 클라우드 비전 API(AWS Rekognition, Google Vision) — 매장별 CCTV 영상을 실시간으로 클라우드에 전송하면 대역폭 비용과 지연 발생. 말레이시아 매장 네트워크 환경에서 안정적 실시간 분석 불가하여 기각. (2) 사전학습 모델 그대로 사용 — 매장 특화 환경(높은 카메라 각도, 다양한 조명 조건)에서 인식률이 낮아 실무 적용 불가하여 기각.
- decision: YOLO 모델을 매장 CCTV 환경에 맞춰 튜닝하고, 베어메탈 Linux 서버를 매장에 직접 설치하는 On-premise 구조 채택. 영상 수집부터 분석, 결과 전달까지 로컬에서 완결되는 파이프라인 구축.
- implementation: 매장 CCTV 영상에서 수집한 데이터로 YOLO 모델을 추가 학습하여 매장 환경 특화 인식률을 확보. 리눅스 서버에서 영상 수집 → YOLO 추론 → 고객 ID 할당 → Tracking → 행동 지표 추출의 파이프라인을 구현. 추출된 데이터는 WebSocket으로 VanillaJS 대시보드에 실시간 전달.
- outcome: 클라우드 의존 없이 현장 설치형 분석 구조를 구현하여, 매장 환경에 맞는 안정적인 영상 수집 및 실시간 분석 체계를 완성. 3인 팀으로 매장 CCTV 기반 고객 행동 분석 시스템을 성공적으로 구축·운영.

**After writing both projects, run full validation:**
```bash
cd /Users/hipark/dev/portfolio && python3 -c "
import json
with open('messages/ko.json') as f: ko = json.load(f)
with open('messages/en.json') as f: en = json.load(f)
projects = ['joshua','dyCms','retailAnalysis']
for p in projects:
    ko_keys = set(ko['ProjectDetail'][p]['challenges'].keys())
    en_keys = set(en['ProjectDetail'][p]['challenges'].keys())
    if ko_keys != en_keys:
        print(f'MISMATCH {p}: ko={ko_keys - en_keys} en={en_keys - ko_keys}')
    else:
        print(f'OK {p}: {ko_keys}')
print('All 3 projects validated')
"
```

Then run `next build` to verify no JSON syntax errors break the build.
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && python3 -c "
import json, re
with open('messages/ko.json') as f: ko = json.load(f)
with open('messages/en.json') as f: en = json.load(f)
for p in ['joshua','dyCms','retailAnalysis']:
    ko_ch = ko['ProjectDetail'][p]['challenges']
    en_ch = en['ProjectDetail'][p]['challenges']
    assert set(ko_ch.keys()) == set(en_ch.keys()), f'{p}: key mismatch ko vs en'
    for ck in ['challenge1','challenge2']:
        for field in ['title','context','alternatives','decision','implementation','outcome']:
            assert field in ko_ch[ck], f'{p}/{ck}: missing {field} in ko'
            assert field in en_ch[ck], f'{p}/{ck}: missing {field} in en'
            assert len(ko_ch[ck][field]) > 20, f'{p}/{ck}/{field}: too short in ko'
            assert len(en_ch[ck][field]) > 20, f'{p}/{ck}/{field}: too short in en'
        assert re.search(r'[0-9%]', ko_ch[ck]['outcome']), f'{p}/{ck}: ko outcome missing number'
        assert re.search(r'[0-9%]', en_ch[ck]['outcome']), f'{p}/{ck}: en outcome missing number'
# Check AI challenges exist for joshua and retailAnalysis (CONT-04)
ai_keywords_ko = ['KoGPT', 'AI', '모델', '추론', '파인튜닝', 'Fine-tuning']
ai_keywords_en = ['KoGPT', 'AI', 'model', 'inference', 'fine-tun', 'YOLO']
joshua_has_ai = any(any(kw in ko['ProjectDetail']['joshua']['challenges'][ck].get('title','') + ko['ProjectDetail']['joshua']['challenges'][ck].get('context','') for kw in ai_keywords_ko) for ck in ['challenge1','challenge2'])
retail_has_ai = any(any(kw in ko['ProjectDetail']['retailAnalysis']['challenges'][ck].get('title','') + ko['ProjectDetail']['retailAnalysis']['challenges'][ck].get('context','') for kw in ai_keywords_ko) for ck in ['challenge1','challenge2'])
assert joshua_has_ai, 'Joshua missing AI challenge (CONT-04)'
assert retail_has_ai, 'Retail Analysis missing AI challenge (CONT-04)'
print('PASS: All 3 projects fully validated including AI challenges')
" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
All 3 career/company projects (Joshua, DY CMS, Retail Analysis) have 2 challenges each in both ko.json and en.json. All outcome fields contain quantitative figures. AI challenges present for Joshua (KoGPT-2) and Retail Analysis (YOLO). Key parity validated. next build passes.
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; json.load(open('messages/ko.json')); json.load(open('messages/en.json')); print('JSON OK')"` — JSON syntax valid
2. Key parity script from 14-RESEARCH.md Pattern 3 — all 3 projects show OK
3. `npx next build` — no errors
4. Manual spot-check: every `outcome` field contains a number or percentage
5. Manual spot-check: every `alternatives` field names a rejected approach with reason
</verification>

<success_criteria>
- Joshua: 2 challenges (IPC + KoGPT-2 AI) in ko + en
- DY CMS: 2 challenges (architecture + RBAC) in ko + en
- Retail Analysis: 2 challenges (VanillaJS + YOLO AI) in ko + en
- All 6 outcome fields contain quantitative figures
- All 6 alternatives fields explicitly reject at least one approach
- Joshua and Retail Analysis each have an AI-specific challenge (CONT-04)
- ko.json and en.json keys match exactly for all 3 projects
- `next build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-authoring/14-01-SUMMARY.md`
</output>
