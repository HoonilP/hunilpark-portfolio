---
phase: 13-component-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - messages/ko.json
  - messages/en.json
  - src/components/projects/ProjectContent.tsx
  - src/components/projects/ChallengeSection.tsx
autonomous: true
requirements:
  - STRC-01
  - STRC-02
  - STRC-03
  - STRC-06

must_haves:
  truths:
    - "Project detail pages render Engineering Challenges section instead of Implementation + Troubleshooting"
    - "Each challenge displays five-part narrative: context, alternatives, decision, implementation, outcome"
    - "All 6 projects render without empty sections in both /ko and /en locales"
    - "i18n JSON files use challenges.challenge1~N schema with title/context/alternatives/decision/implementation/outcome fields"
  artifacts:
    - path: "messages/ko.json"
      provides: "Korean challenges schema for all 6 projects"
      contains: "challenges"
    - path: "messages/en.json"
      provides: "English challenges schema for all 6 projects"
      contains: "challenges"
    - path: "src/components/projects/ChallengeSection.tsx"
      provides: "Server component rendering one challenge with 5 sub-fields"
      exports: ["default"]
    - path: "src/components/projects/ProjectContent.tsx"
      provides: "Rewritten content component with challenges loop"
      contains: "challengeKeys"
  key_links:
    - from: "src/components/projects/ProjectContent.tsx"
      to: "src/components/projects/ChallengeSection.tsx"
      via: "import ChallengeSection"
      pattern: "import.*ChallengeSection"
    - from: "src/components/projects/ProjectContent.tsx"
      to: "messages/ko.json"
      via: "t() calls with challenges keys"
      pattern: "challenges\\.title"
    - from: "src/components/projects/ChallengeSection.tsx"
      to: "src/components/projects/CodeBlock.tsx"
      via: "optional CodeBlock import for code snippets"
      pattern: "import.*CodeBlock"
---

<objective>
Migrate i18n schema from implementation+troubleshooting to challenges structure, and rewrite ProjectContent.tsx + create ChallengeSection.tsx to render the new five-part engineering challenge narrative.

Purpose: Replace the old two-section (Implementation + Troubleshooting) pattern with a unified Engineering Challenges section that presents each challenge as a structured engineering narrative (problem -> approaches -> decision -> implementation -> outcome).
Output: Migrated ko.json/en.json with stub challenges for all 6 projects, rewritten ProjectContent.tsx, new ChallengeSection.tsx.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-infrastructure/13-RESEARCH.md
@.planning/phases/13-component-infrastructure/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (CodeBlock component) -->
From src/components/projects/CodeBlock.tsx:
```typescript
interface CodeBlockProps {
  code: string;
  lang?: string;
}
export default async function CodeBlock({ code, lang = 'typescript' }: CodeBlockProps);
```

<!-- Existing ProjectContent interface -->
From src/components/projects/ProjectContent.tsx:
```typescript
interface ProjectContentProps {
  translationKey: string;
  projectId: string;
}
export default async function ProjectContent({ translationKey, projectId }: ProjectContentProps);
```

<!-- Translation function pattern (server component) -->
```typescript
import { getTranslations } from 'next-intl/server';
const t = await getTranslations('ProjectDetail');
t.has('key') // returns boolean, no error on missing
t('key')     // returns string
```

<!-- Current project translation keys (6 projects) -->
joshua, dyCms, retailAnalysis, scholarlyChain, dinoGo, artWar

<!-- Current old schema keys per project (TO BE REMOVED): -->
implementation: { title, feature1~N: { title, problem, solution, result } }
troubleshooting: { title, issue1~N: { title, problem, solution, result } }

<!-- Keys to KEEP unchanged: title, subtitle, role, teamSize, duration, overview, retrospective -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate i18n JSON schemas — replace implementation+troubleshooting with challenges stubs</name>
  <files>messages/ko.json, messages/en.json</files>
  <action>
Use python3 to safely modify both JSON files atomically. For each of the 6 projects in both ko.json and en.json:

1. **Remove** the `implementation` key and all its children
2. **Remove** the `troubleshooting` key and all its children
3. **Add** a `challenges` key with stub content (1 challenge per project with all 5 fields filled)
4. **Keep** all other keys unchanged: `title`, `subtitle`, `role`, `teamSize`, `duration`, `overview`, `retrospective`

**New challenges schema per project:**
```json
"challenges": {
  "title": "엔지니어링 챌린지",  // or "Engineering Challenges" for en.json
  "challenge1": {
    "title": "[Stub challenge title — Phase 14 will replace]",
    "context": "[Stub — problem definition placeholder]",
    "alternatives": "[Stub — approaches tried placeholder]",
    "decision": "[Stub — comparison/decision placeholder]",
    "implementation": "[Stub — implementation details placeholder]",
    "outcome": "[Stub — quantitative results placeholder]"
  }
}
```

Write a python3 script that:
```python
import json

for filepath in ['messages/ko.json', 'messages/en.json']:
    with open(filepath, 'r', encoding='utf-8') as f:
        data = json.load(f)

    is_ko = 'ko' in filepath
    project_keys = ['joshua', 'dyCms', 'retailAnalysis', 'scholarlyChain', 'dinoGo', 'artWar']

    for pk in project_keys:
        proj = data['ProjectDetail'][pk]
        # Remove old keys
        proj.pop('implementation', None)
        proj.pop('troubleshooting', None)
        # Add new challenges stub
        proj['challenges'] = {
            'title': '엔지니어링 챌린지' if is_ko else 'Engineering Challenges',
            'challenge1': {
                'title': '핵심 기술 챌린지' if is_ko else 'Core Technical Challenge',
                'context': '문제 정의 (Phase 14에서 실제 콘텐츠로 교체 예정)' if is_ko else 'Problem definition (to be replaced with real content in Phase 14)',
                'alternatives': '시도한 접근법 (Phase 14에서 실제 콘텐츠로 교체 예정)' if is_ko else 'Approaches tried (to be replaced with real content in Phase 14)',
                'decision': '비교/결정 근거 (Phase 14에서 실제 콘텐츠로 교체 예정)' if is_ko else 'Comparison and decision rationale (to be replaced with real content in Phase 14)',
                'implementation': '구현 상세 (Phase 14에서 실제 콘텐츠로 교체 예정)' if is_ko else 'Implementation details (to be replaced with real content in Phase 14)',
                'outcome': '정량적 성과 (Phase 14에서 실제 콘텐츠로 교체 예정)' if is_ko else 'Quantitative results (to be replaced with real content in Phase 14)'
            }
        }

    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
        f.write('\n')
```

CRITICAL: Both ko.json and en.json MUST have identical key structures. Every project MUST have at least `challenge1` with all 5 sub-fields to pass `t.has()` checks. Use `ensure_ascii=False` to preserve Korean characters.
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && python3 -c "
import json
for f in ['messages/ko.json', 'messages/en.json']:
    d = json.load(open(f))
    pd = d['ProjectDetail']
    for pk in ['joshua','dyCms','retailAnalysis','scholarlyChain','dinoGo','artWar']:
        p = pd[pk]
        assert 'implementation' not in p, f'{f}:{pk} still has implementation'
        assert 'troubleshooting' not in p, f'{f}:{pk} still has troubleshooting'
        assert 'challenges' in p, f'{f}:{pk} missing challenges'
        c1 = p['challenges']['challenge1']
        for field in ['title','context','alternatives','decision','implementation','outcome']:
            assert field in c1, f'{f}:{pk}:challenge1 missing {field}'
print('All 6 projects in both locales validated')
"</automated>
  </verify>
  <done>Both ko.json and en.json have challenges schema for all 6 projects with challenge1 containing all 5 sub-fields; implementation and troubleshooting keys are removed; all other keys (overview, retrospective, etc.) are unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Create ChallengeSection component and rewrite ProjectContent to render challenges</name>
  <files>src/components/projects/ChallengeSection.tsx, src/components/projects/ProjectContent.tsx</files>
  <action>
1. **Create `src/components/projects/ChallengeSection.tsx`** — server component rendering one challenge:

```typescript
interface ChallengeSectionProps {
  translationKey: string;
  challengeKey: string;
  t: {
    (key: string): string;
    has: (key: string) => boolean;
  };
}

export default function ChallengeSection({ translationKey, challengeKey, t }: ChallengeSectionProps) {
  const prefix = `${translationKey}.challenges.${challengeKey}`;

  // Sub-fields in narrative order
  const fields = [
    { key: 'context', labelKo: '문제 정의', labelEn: 'Problem' },
    { key: 'alternatives', labelKo: '시도한 접근법', labelEn: 'Approaches' },
    { key: 'decision', labelKo: '비교/결정', labelEn: 'Decision' },
    { key: 'implementation', labelKo: '구현', labelEn: 'Implementation' },
    { key: 'outcome', labelKo: '성과', labelEn: 'Outcome' },
  ];

  return (
    <div className="space-y-4 border-l-2 border-neutral-200 dark:border-neutral-700 pl-6">
      <h3 className="text-xl font-semibold text-neutral-900 dark:text-neutral-100">
        {t(`${prefix}.title`)}
      </h3>
      {fields.map(({ key }) => {
        if (!t.has(`${prefix}.${key}`)) return null;
        return (
          <div key={key} className="space-y-1">
            <p className="text-neutral-700 dark:text-neutral-300 leading-relaxed whitespace-pre-line">
              {t(`${prefix}.${key}`)}
            </p>
          </div>
        );
      })}
    </div>
  );
}
```

Note: Do NOT add sub-field labels (context/alternatives/etc.) as visual headings — the research recommends the five-part flow be implicit from content structure. Phase 14 content will use natural narrative transitions. The labels exist only as i18n key names for semantic organization.

The CodeBlock import is NOT wired in this phase — ChallengeSection renders text-only fields. Phase 14 may add optional code snippet rendering via CodeBlock when authoring content that includes code.

2. **Rewrite `src/components/projects/ProjectContent.tsx`** — replace Implementation + Troubleshooting with Engineering Challenges:

Remove:
- `featureKeys` array and implementation section JSX
- `issueKeys` array and troubleshooting section JSX

Add:
- Import `ChallengeSection` from `./ChallengeSection`
- `CHALLENGE_KEYS = ['challenge1', 'challenge2', 'challenge3']` constant
- Engineering Challenges section using `t.has()` pattern

Keep UNCHANGED:
- Overview section
- Architecture/Content Image
- Retrospective section
- Component interface (translationKey, projectId props)
- Import of Image from next/image and getTranslations from next-intl/server

The new challenges section replaces both Implementation and Troubleshooting sections in the same position (between Architecture Image and Retrospective).

```typescript
import ChallengeSection from './ChallengeSection';

const CHALLENGE_KEYS = ['challenge1', 'challenge2', 'challenge3'];

// In JSX, replace Implementation + Troubleshooting with:
{t.has(`${translationKey}.challenges.title`) && (
  <section className="space-y-10">
    <h2 className="text-2xl font-bold text-neutral-900 dark:text-neutral-100">
      {t(`${translationKey}.challenges.title`)}
    </h2>
    {CHALLENGE_KEYS.map((key) => {
      if (!t.has(`${translationKey}.challenges.${key}.title`)) return null;
      return (
        <ChallengeSection
          key={key}
          translationKey={translationKey}
          challengeKey={key}
          t={t}
        />
      );
    })}
  </section>
)}
```
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>ProjectContent.tsx renders Engineering Challenges section (not Implementation/Troubleshooting), ChallengeSection.tsx renders each challenge with 5 sub-fields, all 6 projects render without empty sections in both locales, next build passes</done>
</task>

</tasks>

<verification>
1. `python3` validation script confirms all 6 projects have challenges schema in both ko.json and en.json
2. `grep -c "implementation" messages/ko.json` returns 0 at ProjectDetail level (only inside challenge1.implementation field)
3. `grep -c "troubleshooting" messages/ko.json` returns 0
4. `npx next build` passes without errors
5. `grep "ChallengeSection" src/components/projects/ProjectContent.tsx` confirms import and usage
6. `grep "CHALLENGE_KEYS" src/components/projects/ProjectContent.tsx` confirms challenge loop
</verification>

<success_criteria>
- ko.json and en.json ProjectDetail entries have `challenges` key with at least `challenge1` containing all 5 fields
- ko.json and en.json no longer have `implementation` or `troubleshooting` top-level keys in any project
- ProjectContent.tsx imports and renders ChallengeSection components
- ChallengeSection.tsx renders 5 sub-fields (context, alternatives, decision, implementation, outcome) for each challenge
- All 6 projects render the Engineering Challenges section in both /ko and /en
- `next build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-infrastructure/13-02-SUMMARY.md`
</output>
