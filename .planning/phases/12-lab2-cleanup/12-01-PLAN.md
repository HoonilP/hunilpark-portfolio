---
phase: 12-lab2-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/lab2/layout.tsx
  - src/app/[locale]/lab2/page.tsx
  - src/components/lab2/ (18 files, entire directory)
  - src/components/layout/Header.tsx
  - messages/ko.json
  - messages/en.json
  - package.json
  - package-lock.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05]

must_haves:
  truths:
    - "/ko/lab2 and /en/lab2 routes return 404 (not accessible)"
    - "Header navigation has no lab2 link visible"
    - "ko.json and en.json have no Lab2 namespace or Navigation.lab2 key"
    - "lenis package is not in package.json; gsap, three, @react-three/* remain"
    - "next build completes with zero TypeScript errors"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Header without lab2 link or Box icon import"
      contains: "FlaskConical"
    - path: "messages/ko.json"
      provides: "Korean translations without Lab2 namespace"
    - path: "messages/en.json"
      provides: "English translations without Lab2 namespace"
    - path: "package.json"
      provides: "Dependencies without lenis, with gsap and three intact"
  key_links:
    - from: "Header.tsx"
      to: "lucide-react"
      via: "import { FlaskConical } — Box removed"
      pattern: "import.*FlaskConical.*from 'lucide-react'"
    - from: "package.json"
      to: "node_modules"
      via: "lenis removed, gsap/three retained"
      pattern: "gsap.*@gsap/react.*three.*@react-three"
---

<objective>
Remove all lab2-related code, routes, translations, navigation, and the lenis package from the codebase.

Purpose: lab2 (3D Studio experience) is being retired in v4.0. Complete removal eliminates dead code and unused dependencies, preparing the codebase for the project detail page enhancement focus.
Output: Clean codebase with no lab2 traces, passing `next build` with zero errors.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lab2-cleanup/12-RESEARCH.md

Key facts from research:
- lab2 route: `src/app/[locale]/lab2/` (layout.tsx, page.tsx)
- lab2 components: 18 files under `src/components/lab2/` (full tree in RESEARCH.md)
- Header.tsx: line 5 imports `{ FlaskConical, Box }` from lucide-react — `Box` is lab2-only
- Header.tsx: lines 72-78 contain the lab2 Link block
- ko.json/en.json: top-level `"Lab2"` namespace + `"Navigation"."lab2"` key
- `lenis` exclusively used in `src/components/lab2/` — safe to uninstall
- GSAP used in `src/components/HorizontalScrollWrapper.tsx` — do NOT remove
- Three.js/R3F used in `/lab` (lab1) — do NOT remove
- npm cache workaround: use `--cache /tmp/npm-cache-temp`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete lab2 files, navigation link, and translation keys</name>
  <files>
    src/app/[locale]/lab2/ (delete entire directory)
    src/components/lab2/ (delete entire directory)
    src/components/layout/Header.tsx
    messages/ko.json
    messages/en.json
  </files>
  <action>
    **Step 1 — Safety check (MUST run first):**
    Run `grep -rn "from 'gsap'\|from '@gsap/react'" src/` to confirm GSAP is used in HorizontalScrollWrapper.tsx (not in lab2). This confirms GSAP must NOT be removed.
    Run `grep -rn "from 'lenis'" src/` to confirm lenis is ONLY in `src/components/lab2/` files. This confirms lenis is safe to remove.

    **Step 2 — Delete lab2 route directory:**
    `rm -rf src/app/[locale]/lab2/`
    This removes layout.tsx and page.tsx, eliminating /ko/lab2 and /en/lab2 routes.

    **Step 3 — Delete lab2 components directory:**
    `rm -rf src/components/lab2/`
    This removes all 18 files: Lab2Scene.tsx, EmptyScene.tsx, config/chapters.ts, hooks/useScrollProgress.ts, hooks/useViewportWidth.ts, scene/CameraRig.tsx, scene/ParticleField.tsx, scene/SceneManager.tsx, scene/chapters/IntroScene.tsx, scene/chapters/Project1-5Scene.tsx, scene/shared/TexturePlane.tsx, ui/LenisProvider.tsx, ui/LoadingScreen.tsx, ui/ViewportGate.tsx.

    **Step 4 — Edit Header.tsx:**
    - Change import line from `import { FlaskConical, Box } from 'lucide-react'` to `import { FlaskConical } from 'lucide-react'`
    - Remove the lab2 Link block (the entire `<Link href="/lab2" ...>` element containing `<Box className="w-4 h-4" />`). This is approximately lines 72-78 but verify exact location by searching for `href="/lab2"`.
    - Do NOT touch the `/lab` link (FlaskConical icon) — it must remain.

    **Step 5 — Edit messages/ko.json:**
    - Remove the entire top-level `"Lab2": { ... }` object (keys: backToHome, viewportGate, loading)
    - Remove `"lab2": "Studio"` from inside the `"Navigation"` object
    - Ensure the trailing comma on the line before the removed key is handled correctly (valid JSON)

    **Step 6 — Edit messages/en.json:**
    - Remove the entire top-level `"Lab2": { ... }` object (keys: backToHome, viewportGate, loading)
    - Remove `"lab2": "Studio"` from inside the `"Navigation"` object
    - Ensure valid JSON after removal

    **Anti-patterns to AVOID:**
    - Do NOT delete or modify anything in `src/components/lab/` (lab1 — still in use)
    - Do NOT remove FlaskConical import or /lab Link from Header.tsx
    - Do NOT remove GSAP, Three.js, or R3F packages
  </action>
  <verify>
    <automated>
      test ! -d src/app/\[locale\]/lab2 && test ! -d src/components/lab2 && echo "PASS: directories deleted" || echo "FAIL: directories still exist"
      grep -c "lab2" src/components/layout/Header.tsx | grep -q "^0$" && echo "PASS: no lab2 in Header" || echo "FAIL: lab2 still in Header"
      python3 -c "import json; d=json.load(open('messages/ko.json')); assert 'Lab2' not in d; assert 'lab2' not in d.get('Navigation',{}); print('PASS: ko.json clean')"
      python3 -c "import json; d=json.load(open('messages/en.json')); assert 'Lab2' not in d; assert 'lab2' not in d.get('Navigation',{}); print('PASS: en.json clean')"
    </automated>
  </verify>
  <done>
    - src/app/[locale]/lab2/ directory does not exist
    - src/components/lab2/ directory does not exist
    - Header.tsx has no lab2 references, no Box import, FlaskConical and /lab link intact
    - ko.json has no "Lab2" namespace and no "Navigation.lab2" key, is valid JSON
    - en.json has no "Lab2" namespace and no "Navigation.lab2" key, is valid JSON
  </done>
</task>

<task type="auto">
  <name>Task 2: Uninstall lenis and verify build</name>
  <files>
    package.json
    package-lock.json
  </files>
  <action>
    **Step 1 — Uninstall lenis:**
    Run: `npm uninstall lenis --cache /tmp/npm-cache-temp`
    This removes lenis from package.json dependencies and cleans the lock file.

    **Step 2 — Verify retained packages:**
    Run: `grep -E '"gsap"|"@gsap/react"|"three"|"@react-three/fiber"|"@react-three/drei"' package.json`
    All 5 packages must still be present. If any are missing, STOP — something went wrong.

    **Step 3 — Clean build cache and run build:**
    Run: `rm -rf .next && npx next build`
    The build must complete with zero TypeScript errors and zero missing module errors.
    Expected: successful build output with no errors.

    **Step 4 — Verify lab2 route artifacts are gone:**
    Run: `test ! -d .next/server/app/\[locale\]/lab2 && echo "PASS: no lab2 build artifacts" || echo "FAIL: lab2 build artifacts exist"`

    **Anti-patterns to AVOID:**
    - Do NOT run `npm uninstall gsap` or `npm uninstall @gsap/react`
    - Do NOT run `npm uninstall three` or `npm uninstall @react-three/fiber` or `npm uninstall @react-three/drei`
  </action>
  <verify>
    <automated>
      grep -q '"lenis"' package.json && echo "FAIL: lenis still in package.json" || echo "PASS: lenis removed"
      grep -qE '"gsap"|"@gsap/react"' package.json && echo "PASS: gsap retained" || echo "FAIL: gsap missing"
      grep -qE '"three"|"@react-three/fiber"' package.json && echo "PASS: three.js retained" || echo "FAIL: three.js missing"
      npx next build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - lenis is not in package.json
    - gsap, @gsap/react, three, @react-three/fiber, @react-three/drei are all still in package.json
    - `next build` completes successfully with zero errors
    - No lab2 build artifacts in .next/server/app/
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification sequence:

1. **Route removal:** `test ! -d src/app/\[locale\]/lab2 && echo "PASS"` — directories must not exist
2. **Component removal:** `test ! -d src/components/lab2 && echo "PASS"` — directory must not exist
3. **Header clean:** `grep "lab2\|Box" src/components/layout/Header.tsx` — must return nothing
4. **i18n clean (ko):** `python3 -c "import json; d=json.load(open('messages/ko.json')); assert 'Lab2' not in d and 'lab2' not in d['Navigation']"`
5. **i18n clean (en):** `python3 -c "import json; d=json.load(open('messages/en.json')); assert 'Lab2' not in d and 'lab2' not in d['Navigation']"`
6. **lenis gone:** `grep -q '"lenis"' package.json && echo FAIL || echo PASS`
7. **GSAP retained:** `grep -q '"gsap"' package.json && echo PASS || echo FAIL`
8. **Three.js retained:** `grep -q '"three"' package.json && echo PASS || echo FAIL`
9. **Build passes:** `npx next build` exits 0
</verification>

<success_criteria>
- /ko/lab2 and /en/lab2 are inaccessible (route directory deleted)
- Header shows no lab2 navigation link
- Lab2 namespace and Navigation.lab2 key absent from both ko.json and en.json
- lenis removed from package.json; gsap, three, @react-three/* all retained
- `next build` passes clean with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-lab2-cleanup/12-01-SUMMARY.md`
</output>
