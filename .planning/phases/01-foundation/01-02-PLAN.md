---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/LanguageToggle.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/layout.tsx
autonomous: false

must_haves:
  truths:
    - "User can toggle between Korean and English using a visible button"
    - "Site displays correctly on mobile viewport (375px width)"
    - "Site displays correctly on desktop viewport (1280px width)"
    - "Language toggle changes URL from /ko to /en and vice versa"
    - "Page content updates to the selected language after toggle"
  artifacts:
    - path: "src/components/LanguageToggle.tsx"
      provides: "Client component for switching locale"
      contains: "useRouter"
    - path: "src/app/[locale]/page.tsx"
      provides: "Responsive home page with bilingual content and language toggle"
      contains: "LanguageToggle"
  key_links:
    - from: "src/components/LanguageToggle.tsx"
      to: "src/i18n/navigation.ts"
      via: "useRouter and usePathname for locale switching"
      pattern: "useRouter|usePathname"
    - from: "src/components/LanguageToggle.tsx"
      to: "src/i18n/routing.ts"
      via: "routing.locales for available locales"
      pattern: "routing\\.locales|routing"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/LanguageToggle.tsx"
      via: "import and render in page"
      pattern: "LanguageToggle"
---

<objective>
Build a responsive bilingual home page with a language toggle button, completing the Phase 1 foundation by delivering all four success criteria: bilingual routing, responsive layout, design system usage, and language switching.

Purpose: This plan delivers the user-visible output of Phase 1 -- a working page that proves the entire foundation (Next.js, Tailwind, i18n) is correctly wired together.
Output: A responsive home page with language toggle, demonstrating design tokens, bilingual content, and mobile-first layout.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create language toggle and responsive home page</name>
  <files>
    src/components/LanguageToggle.tsx
    src/app/[locale]/page.tsx
    src/app/[locale]/layout.tsx
  </files>
  <action>
    1. Create `src/components/LanguageToggle.tsx`:
       - Mark as 'use client' (uses hooks)
       - Import `useRouter`, `usePathname` from '@/i18n/navigation'
       - Import `useLocale` from 'next-intl'
       - Import `routing` from '@/i18n/routing'
       - Component renders a button that:
         - Shows "EN" when current locale is 'ko', shows "KO" when current locale is 'en'
         - On click, calls `router.replace(pathname, {locale: targetLocale})` where targetLocale is the opposite of current locale
         - Styled with Tailwind: border border-neutral-300, rounded-md, px-3 py-1.5, text-sm, font-medium, text-neutral-700, hover:bg-neutral-100, transition-colors
         - Has aria-label for accessibility using the translation from Common.languageLabel
       - Import `useTranslations` from 'next-intl' for the aria-label

    2. Update `src/app/[locale]/page.tsx` to be a proper responsive landing page:
       - Keep as server component (no 'use client')
       - Await params, call setRequestLocale
       - Use useTranslations('Home') for content
       - Layout structure (mobile-first, responsive):
         - Main container: `min-h-screen flex flex-col`
         - Top-right language toggle area: `flex justify-end p-4 sm:p-6 lg:p-8`
           - Render `<LanguageToggle />` here
         - Center content area: `flex-1 flex flex-col items-center justify-center px-4 sm:px-6 lg:px-8`
           - Title (name): `text-3xl sm:text-4xl lg:text-5xl font-bold text-neutral-900 tracking-tight`
           - Subtitle (role): `mt-2 text-lg sm:text-xl lg:text-2xl text-neutral-500 font-light`
           - Description: `mt-4 text-base sm:text-lg text-neutral-600 max-w-md text-center`
           - Subtle divider: `mt-6 w-12 h-px bg-neutral-300`
           - Placeholder text for next phases: `mt-6 text-sm text-neutral-400` saying "More sections coming soon" / "더 많은 섹션이 곧 추가됩니다" (add to translation files)
         - Footer area: `p-4 sm:p-6 lg:p-8 text-center text-sm text-neutral-400`
       - Use design tokens: text-primary-500 for subtle accent on name or divider if it looks good

    3. Update `src/app/[locale]/layout.tsx` if needed:
       - Ensure the body has `bg-white text-neutral-900` base styles
       - Ensure proper viewport meta for responsive design (Next.js adds this by default via metadata)
       - Add `antialiased` class to body for smooth font rendering

    4. Update translation files if new keys are added (e.g., "comingSoon" key under Home namespace). Add to BOTH ko.json and en.json:
       ```json
       "Home": {
         ...existing keys...,
         "comingSoon": "더 많은 섹션이 곧 추가됩니다" // ko
         "comingSoon": "More sections coming soon"      // en
       }
       ```

    Design principles:
    - Mobile-first: unprefixed utilities target mobile, sm:/lg: for larger screens
    - Minimal and clean: lots of whitespace, subtle colors, no animations
    - Pretendard font should be visually apparent (check font loads)
    - The page should feel like a real portfolio landing, not a boilerplate template
  </action>
  <verify>
    1. `npm run dev` starts without errors
    2. Visit `/ko` -- Korean content renders, LanguageToggle shows "EN"
    3. Click "EN" button -- URL changes to `/en`, content switches to English, button shows "KO"
    4. Click "KO" button -- URL changes back to `/ko`
    5. Resize browser to 375px width -- layout remains clean, text sizes adjust
    6. Resize browser to 1280px width -- layout uses wider spacing, larger text
    7. `npm run build` succeeds
  </verify>
  <done>
    Language toggle switches between /ko and /en. Page content updates to match locale. Layout is responsive across mobile and desktop. Tailwind design tokens (colors, font) are visually used. The page looks like a minimal, clean portfolio landing page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 foundation: Next.js project with Tailwind v4 design system, next-intl bilingual routing (/ko and /en), responsive layout, and language toggle button.</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project directory
    2. Open http://localhost:3000 -- should redirect to /ko
    3. Verify Korean content displays: "박훈일", "프론트엔드 개발자"
    4. Click the "EN" button (top-right) -- URL should change to /en
    5. Verify English content displays: "Hunil Park", "Frontend Developer"
    6. Click "KO" button to switch back
    7. Open browser DevTools, toggle device toolbar to iPhone SE (375px) -- verify layout looks clean
    8. Toggle to desktop (1280px+) -- verify text sizes are larger and spacing is wider
    9. Verify the Pretendard font is loading (check Network tab for pretendard CDN request)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Language toggle button is visible and functional
2. /ko shows Korean, /en shows English
3. Toggle navigates between locales without page reload flash
4. Mobile layout (375px): stacked, readable, proper spacing
5. Desktop layout (1280px+): centered, larger text, wider padding
6. Pretendard font loads and renders
7. Design tokens (color-primary, color-neutral) are used in the UI
8. `npm run build` succeeds with no warnings
</verification>

<success_criteria>
- User can toggle between Korean and English via a button
- Both /ko and /en routes serve correct content
- Layout is responsive (mobile-first with sm: and lg: breakpoints)
- Tailwind design tokens are visually applied (colors, typography)
- Human verification passes (approved by user)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
