---
phase: 06-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/layout/Header.tsx
autonomous: false
requirements:
  - FOUND-05

must_haves:
  truths:
    - "Main site header has a visible link to /lab2 (labeled 'Studio') alongside existing Lab link"
    - "Navigating /lab2 -> / -> /lab2 ten times does not cause WebGL context loss or black canvas"
    - "Three.js bundle is isolated to lab/lab2 route chunks (not in main page bundle)"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "/lab2 navigation link with Studio label and distinct icon"
      contains: "lab2"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "/lab2"
      via: "Link component from @/i18n/navigation"
      pattern: 'href="/lab2"'
---

<objective>
Add /lab2 navigation link to the main site header and verify WebGL context lifecycle stability.

Purpose: Complete the integration of /lab2 into the main site navigation and confirm the critical FOUND-05 requirement — that repeated navigation between /lab2 and the main site does not leak WebGL contexts.

Output: Header with Studio link, verified WebGL lifecycle stability.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation/06-01-SUMMARY.md

Reference files:
@src/components/layout/Header.tsx
@messages/ko.json
@messages/en.json

<interfaces>
<!-- From Header.tsx — current Lab link pattern to mirror -->
From src/components/layout/Header.tsx (lines 63-73):
```typescript
<div className="flex items-center gap-3">
  <ThemeToggle />
  <Link
    href="/lab"
    className="rounded-lg p-2 hover:bg-neutral-100 dark:hover:bg-neutral-800 transition-colors text-neutral-600 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-neutral-100"
    title={t('lab')}
  >
    <FlaskConical className="w-4 h-4" />
  </Link>
  <LanguageToggle />
```

<!-- i18n keys added in Plan 01 -->
Navigation namespace already has "lab2": "Studio" (added in Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /lab2 link to Header navigation</name>
  <files>src/components/layout/Header.tsx</files>
  <action>
Add a /lab2 link next to the existing /lab link in the Header component. Use a different lucide-react icon to visually distinguish from the Lab link.

1. Import `{Box}` from `'lucide-react'` (in addition to existing `FlaskConical` import). The Box icon represents 3D/spatial — fits the interactive 3D portfolio concept.

2. Add the link immediately after the existing `/lab` Link block (before `<LanguageToggle />`):
```tsx
<Link
  href="/lab2"
  className="rounded-lg p-2 hover:bg-neutral-100 dark:hover:bg-neutral-800 transition-colors text-neutral-600 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-neutral-100"
  title={t('lab2')}
>
  <Box className="w-4 h-4" />
</Link>
```

3. Use the exact same className as the existing /lab link for visual consistency.

4. The `t('lab2')` references the "lab2": "Studio" key already added to Navigation namespace in Plan 01.

**Do NOT modify any other part of the Header.** Only add the import and the Link block.
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && grep -c 'href="/lab2"' src/components/layout/Header.tsx && grep -c "Box" src/components/layout/Header.tsx</automated>
  </verify>
  <done>
    - Header has a /lab2 link with Box icon next to the existing /lab link
    - Link uses t('lab2') for title (shows "Studio")
    - Visual style matches existing /lab link
  </done>
</task>

<task type="auto">
  <name>Task 2: Build verification and bundle analysis</name>
  <files></files>
  <action>
1. Run `npm run build` to confirm everything compiles cleanly.

2. Examine the build output to verify Three.js bundle isolation:
   - Check `.next/analyze/` or build output for chunk names
   - Search for "three" in the build manifest: `grep -r "three" .next/build-manifest.json` or similar
   - Confirm Three.js appears only in lab/lab2-related chunks, NOT in the main page or global chunks

3. If `@next/bundle-analyzer` is not installed, try:
   ```bash
   npx next experimental-analyze
   ```
   Or analyze the build output manually by checking chunk sizes in the build summary.

4. If Three.js leaks into the main bundle, investigate:
   - Ensure Lab2Scene is loaded with `dynamic(..., { ssr: false })`
   - Ensure no Three.js imports exist outside of /lab and /lab2 component trees
   - Ensure next.config.ts `transpilePackages: ['three']` is present

Report the results in the summary. Exact chunk names/sizes are not required — just confirm isolation.
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Build succeeds with no errors
    - Three.js is not loaded on the main page route (isolated to lab/lab2 chunks)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify /lab2 end-to-end in browser</name>
  <files></files>
  <action>
Human verification of the complete /lab2 experience. Start dev server with `npm run dev` and verify in browser:

1. Visit http://localhost:3000/ko/lab2 — should see loading percentage then dark canvas
2. Visit http://localhost:3000/en/lab2 — same experience in English
3. Resize browser below 1024px — should see viewport gate message with back link
4. Resize back above 1024px — canvas should reappear (no black screen)
5. Click back-to-home link — should navigate to main site
6. Check main site header — should see Box icon (Studio link) next to Flask icon (Lab)
7. Click Studio icon — should navigate to /lab2
8. **CRITICAL — WebGL lifecycle test:** Navigate /lab2 -> / -> /lab2 at least 5 times rapidly. Canvas should never go black or show errors.
9. Open browser console — check for WebGL context warnings or errors

What was built:
- WebGL canvas rendering an empty dark scene
- Minimal percentage loading screen (number only, fades out)
- Viewport gate at < 1024px
- Back-to-home link (top-left corner)
- Studio link in main site header (Box icon)
- Bilingual support (ko/en)
  </action>
  <verify>
    <automated>echo "Human verification required — run npm run dev and test in browser"</automated>
  </verify>
  <done>
    - User confirms /ko/lab2 and /en/lab2 render canvas correctly
    - Viewport gate works at < 1024px with real-time resize response
    - WebGL context survives 5+ navigation cycles without errors
    - Studio link visible in header with correct icon
  </done>
</task>

</tasks>

<verification>
1. Header has /lab2 link with Box icon and "Studio" title
2. `npm run build` succeeds
3. Three.js bundle isolated to lab/lab2 chunks
4. 10-cycle /lab2 ↔ / navigation shows no WebGL context loss
5. Both locales work (/ko/lab2, /en/lab2)
6. Viewport gate activates at < 1024px
</verification>

<success_criteria>
- Main site header shows Lab2 "Studio" link with distinct icon
- Build succeeds with no errors
- Repeated navigation between /lab2 and main site (10+ cycles) produces no WebGL errors or black canvas
- Browser console shows no WebGL context warnings during navigation testing
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-02-SUMMARY.md`
</output>
