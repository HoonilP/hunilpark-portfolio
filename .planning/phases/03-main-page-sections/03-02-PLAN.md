---
phase: 03-main-page-sections
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - messages/ko.json
  - messages/en.json
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/ProjectsSection.tsx
autonomous: true

must_haves:
  truths:
    - "User sees skills section with tech stack categorized into Frontend, Backend, DevOps, DB groups"
    - "User sees 3 project cards (Joshua AI Agent, DY Microfinance CMS, Retail Store Analysis) with title, description, tech badges"
    - "Skills and Projects sections display correct content when switching between Korean and English"
  artifacts:
    - path: "src/components/sections/SkillsSection.tsx"
      provides: "Skills section with categorized tech stack badges"
      min_lines: 40
    - path: "src/components/sections/ProjectsSection.tsx"
      provides: "Projects section with 3 project cards using Card and Badge components"
      min_lines: 50
  key_links:
    - from: "src/components/sections/SkillsSection.tsx"
      to: "src/components/ui/Badge.tsx"
      via: "import Badge"
      pattern: "import.*Badge"
    - from: "src/components/sections/ProjectsSection.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import Card"
      pattern: "import.*Card"
    - from: "src/components/sections/ProjectsSection.tsx"
      to: "src/components/ui/Badge.tsx"
      via: "import Badge for tech stack"
      pattern: "import.*Badge"
---

<objective>
Build the Skills and Projects sections as async server components, composing existing Card and Badge UI components with full bilingual content.

Purpose: These sections are the core value proposition — they show WHAT the developer can do (skills) and WHERE they applied it (projects). Recruiters scan these sections to assess technical fit.
Output: Two section components (SkillsSection, ProjectsSection) with bilingual translation keys added to ko.json/en.json.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-main-page-sections/03-RESEARCH.md
@src/components/ui/Card.tsx
@src/components/ui/Badge.tsx
@messages/ko.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Skills and Projects translation keys</name>
  <files>messages/ko.json, messages/en.json</files>
  <action>
    Add Skills and Projects sections to both translation files. Keep ALL existing keys intact (Home, Navigation, Common, Footer, Hero, About, Contact from Plan 01).

    **ko.json additions:**
    ```json
    "Skills": {
      "sectionTitle": "기술 스택",
      "frontend": "프론트엔드",
      "backend": "백엔드",
      "devops": "DevOps",
      "database": "데이터베이스"
    },
    "Projects": {
      "sectionTitle": "프로젝트",
      "viewDetails": "자세히 보기",
      "joshua": {
        "title": "Joshua AI Agent",
        "description": "KoGPT-2 기반 카피라이팅 AI 에이전트. Electron과 Angular로 크로스플랫폼 데스크톱 앱을 구현하고 Stripe 결제를 연동했습니다.",
        "period": "2022.06 - 2023.03"
      },
      "dyCms": {
        "title": "DY Microfinance CMS",
        "description": "Next.js와 NestJS 기반 고객 관리 시스템. 관리자 대시보드를 구현하고 회계 프로세스 약 90%를 자동화했습니다.",
        "period": "2024.07 - 2025.06"
      },
      "retailAnalysis": {
        "title": "리테일 매장 고객 행동 분석",
        "description": "YOLO 기반 시각지능으로 매장 내 고객 동선을 트래킹하고 VanillaJS 대시보드로 분석 결과를 시각화했습니다.",
        "period": "2022.11 - 2023.04"
      }
    }
    ```

    **en.json additions:**
    ```json
    "Skills": {
      "sectionTitle": "Tech Stack",
      "frontend": "Frontend",
      "backend": "Backend",
      "devops": "DevOps",
      "database": "Database"
    },
    "Projects": {
      "sectionTitle": "Projects",
      "viewDetails": "View Details",
      "joshua": {
        "title": "Joshua AI Agent",
        "description": "AI copywriting agent powered by KoGPT-2. Built a cross-platform desktop app with Electron and Angular, integrated Stripe payments.",
        "period": "Jun 2022 - Mar 2023"
      },
      "dyCms": {
        "title": "DY Microfinance CMS",
        "description": "Customer management system built with Next.js and NestJS. Implemented an admin dashboard and automated ~90% of accounting processes.",
        "period": "Jul 2024 - Jun 2025"
      },
      "retailAnalysis": {
        "title": "Retail Store Customer Analysis",
        "description": "Visual intelligence system using YOLO for in-store customer tracking, with a VanillaJS dashboard for analytics visualization.",
        "period": "Nov 2022 - Apr 2023"
      }
    }
    ```

    **Content sourced from:** PROJECT.md Context section (경력 data for projects). Do NOT invent additional details.

    **Tech stacks per project (for Badge rendering — NOT translated, kept as-is):**
    - Joshua: Electron, Angular, FastAPI, PostgreSQL, KoGPT-2, Stripe
    - DY CMS: Next.js, NestJS, PostgreSQL, TypeScript
    - Retail Analysis: Pytorch, YOLO, VanillaJS
  </action>
  <verify>
    - Both JSON files valid: `cat messages/ko.json | python3 -c "import sys,json;json.load(sys.stdin);print('valid')"`
    - Both files contain Skills and Projects keys
    - Existing keys (Home, Navigation, Common, Footer, Hero, About, Contact) still present
  </verify>
  <done>ko.json and en.json contain complete bilingual content for Skills and Projects sections</done>
</task>

<task type="auto">
  <name>Task 2: Create SkillsSection and ProjectsSection components</name>
  <files>src/components/sections/SkillsSection.tsx, src/components/sections/ProjectsSection.tsx</files>
  <action>
    **SkillsSection.tsx:**
    - Async server component using `getTranslations('Skills')`
    - Section id="skills" with py-20 padding, max-w-4xl mx-auto px-4
    - Section title (h2) from t('sectionTitle'), text-3xl font-bold md:text-4xl
    - Define skill categories as array of objects with translated names and skill strings:
      - Frontend: t('frontend') — TypeScript, React, Next.js, Angular, VanillaJS
      - Backend: t('backend') — NestJS, FastAPI, Python
      - DevOps: t('devops') — AWS, Docker, Kubernetes
      - Database: t('database') — MySQL, PostgreSQL
    - Render as grid: grid-cols-1 md:grid-cols-2 gap-8
    - Each category: h3 (text-lg font-semibold mb-3) + flex flex-wrap gap-2 of Badge components
    - Import Badge from '@/components/ui/Badge' (or '@/components/ui')
    - Use stable key for Badge map: skill string itself (unique within category)
    - Use category id string as key for category map (e.g., "frontend", "backend") — NOT array index
    - Per SKILL-02 (skills-to-projects connection): Add a subtle subtitle under each skill category showing which projects used those skills. E.g., under Frontend: "Joshua AI Agent, DY Microfinance CMS" — use t() for project names. This satisfies the connection requirement simply.

    **ProjectsSection.tsx:**
    - Async server component using `getTranslations('Projects')`
    - Section id="projects" with py-20 padding, max-w-5xl mx-auto px-4
    - Section title (h2) from t('sectionTitle'), text-3xl font-bold md:text-4xl mb-8
    - Define 3 projects as array of objects with id, translationKey, techStack array:
      ```
      { id: 'joshua', key: 'joshua', tech: ['Electron', 'Angular', 'FastAPI', 'PostgreSQL'] }
      { id: 'dy-cms', key: 'dyCms', tech: ['Next.js', 'NestJS', 'PostgreSQL', 'TypeScript'] }
      { id: 'retail', key: 'retailAnalysis', tech: ['Pytorch', 'YOLO', 'VanillaJS'] }
      ```
    - Render as grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
    - Each project uses Card component with hover prop:
      - Inside Card: period (text-sm text-neutral-500), title (h3 text-xl font-semibold), description (text-neutral-600 dark:text-neutral-400 mt-2 mb-4), tech badges row (flex flex-wrap gap-2, each Badge)
    - Import Card from '@/components/ui/Card', Badge from '@/components/ui/Badge'
    - Use project.id as key (stable, not array index)
    - No thumbnail images yet (use a subtle colored placeholder div: h-32 rounded-t-lg bg-neutral-100 dark:bg-neutral-900 mb-4 — or omit entirely for cleaner look)
    - Omit thumbnails for now — border-only Card with text content is clean enough. Placeholder images can be added in Phase 4.

    **Both components must:**
    - Be async functions (not 'use client')
    - Use `getTranslations` from 'next-intl/server'
    - Follow mobile-first responsive design
    - Use existing design tokens and border-only card aesthetic
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors
    - Both files export async default functions
    - SkillsSection imports Badge, ProjectsSection imports Card and Badge
    - No 'use client' directive in either file
  </verify>
  <done>SkillsSection renders 4 categorized skill groups with Badge components; ProjectsSection renders 3 project cards with Card and Badge components; both support bilingual content</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `npx tsc --noEmit` passes type checking
3. SkillsSection shows 4 categories with correct skills as Badges
4. ProjectsSection shows 3 project cards with titles, descriptions, periods, and tech badges
5. All content from PROJECT.md only — no invented details
</verification>

<success_criteria>
- Skills section displays 4 categorized groups (Frontend, Backend, DevOps, DB) with Badge components
- Each skill category shows which projects used those skills (SKILL-02 requirement)
- Projects section displays 3 project cards with Card component, titles, descriptions, and tech Badges
- All content switches correctly between Korean and English
- Tech stack matches exactly what's in PROJECT.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-main-page-sections/03-02-SUMMARY.md`
</output>
