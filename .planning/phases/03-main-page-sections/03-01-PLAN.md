---
phase: 03-main-page-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - messages/ko.json
  - messages/en.json
  - src/components/sections/HeroSection.tsx
  - src/components/sections/AboutSection.tsx
  - src/components/sections/ContactSection.tsx
autonomous: true

must_haves:
  truths:
    - "User sees hero section with name (박훈일), title (프론트엔드 개발자), one-line intro, and contact links (email, GitHub, Velog)"
    - "User sees about section with frontend-focused self-introduction mentioning overseas experience"
    - "User sees contact section with email, phone, GitHub, and Velog links"
    - "All three sections display correct content when switching between Korean and English"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section with name, title, intro, social links"
      min_lines: 30
    - path: "src/components/sections/AboutSection.tsx"
      provides: "About section with bilingual self-introduction"
      min_lines: 25
    - path: "src/components/sections/ContactSection.tsx"
      provides: "Contact section with email, phone, GitHub, Velog"
      min_lines: 40
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "messages/ko.json"
      via: "getTranslations('Hero')"
      pattern: "getTranslations.*Hero"
    - from: "src/components/sections/ContactSection.tsx"
      to: "messages/ko.json"
      via: "getTranslations('Contact')"
      pattern: "getTranslations.*Contact"
---

<objective>
Build the Hero, About, and Contact sections as async server components with full bilingual support, and install lucide-react for icons.

Purpose: Deliver the first and last visible sections of the portfolio — the hero that introduces the person and the contact section that enables outreach. These are the highest-impact sections for a recruiter scanning the page.
Output: Three section components (HeroSection, AboutSection, ContactSection), lucide-react installed, and bilingual translation keys in ko.json/en.json.
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-main-page-sections/03-RESEARCH.md
@src/components/ui/index.ts
@src/app/[locale]/page.tsx
@messages/ko.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and add Hero/About/Contact translation keys</name>
  <files>package.json, messages/ko.json, messages/en.json</files>
  <action>
    1. Install lucide-react: `npm install lucide-react`

    2. Add bilingual translation keys to messages/ko.json and messages/en.json. Keep existing keys (Home, Navigation, Common, Footer) intact. Add these new sections:

    **ko.json additions:**
    ```json
    "Hero": {
      "name": "박훈일",
      "title": "프론트엔드 개발자",
      "intro": "깔끔하고 정교한 웹 경험을 만듭니다",
      "resumeLink": "이력서 보기"
    },
    "About": {
      "sectionTitle": "소개",
      "paragraph1": "사용자 중심의 웹 인터페이스를 설계하고 구현하는 프론트엔드 개발자입니다. React, Next.js, TypeScript를 활용한 대시보드 및 관리 시스템 개발 경험을 보유하고 있으며, 복잡한 비즈니스 로직을 직관적인 UI로 풀어내는 데 관심이 있습니다.",
      "paragraph2": "프랑스, 미얀마, 말레이시아에서의 해외 경험을 통해 다국어·다문화 환경에서의 협업 역량을 갖추었습니다. 영어(TOEIC 930)와 프랑스어 비즈니스 레벨의 언어 능력을 바탕으로 글로벌 팀과의 원활한 커뮤니케이션이 가능합니다."
    },
    "Contact": {
      "sectionTitle": "연락처",
      "description": "함께 일하고 싶으시다면 편하게 연락주세요.",
      "email": "이메일",
      "phone": "전화",
      "github": "GitHub",
      "velog": "Velog"
    }
    ```

    **en.json additions:**
    ```json
    "Hero": {
      "name": "Hunil Park",
      "title": "Frontend Developer",
      "intro": "Crafting clean and refined web experiences",
      "resumeLink": "View Resume"
    },
    "About": {
      "sectionTitle": "About",
      "paragraph1": "I'm a frontend developer who designs and implements user-centric web interfaces. With hands-on experience building dashboards and management systems using React, Next.js, and TypeScript, I focus on translating complex business logic into intuitive UIs.",
      "paragraph2": "Having lived and worked in France, Myanmar, and Malaysia, I bring cross-cultural collaboration skills to every project. With strong English proficiency (TOEIC 930) and business-level French, I communicate effectively in global team settings."
    },
    "Contact": {
      "sectionTitle": "Contact",
      "description": "Interested in working together? Feel free to reach out.",
      "email": "Email",
      "phone": "Phone",
      "github": "GitHub",
      "velog": "Velog"
    }
    ```

    **Important:** Use PDF data ONLY from PROJECT.md Context section. Do NOT invent additional content.
  </action>
  <verify>
    - `npm ls lucide-react` shows installed version
    - `cat messages/ko.json | python3 -c "import sys,json;json.load(sys.stdin);print('valid')"` outputs "valid"
    - `cat messages/en.json | python3 -c "import sys,json;json.load(sys.stdin);print('valid')"` outputs "valid"
    - Both JSON files contain Hero, About, Contact keys alongside existing Home, Navigation, Common, Footer keys
  </verify>
  <done>lucide-react installed, both ko.json and en.json contain valid bilingual content for Hero, About, and Contact sections</done>
</task>

<task type="auto">
  <name>Task 2: Create HeroSection, AboutSection, and ContactSection components</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/AboutSection.tsx, src/components/sections/ContactSection.tsx</files>
  <action>
    Create `src/components/sections/` directory and build three async server components:

    **HeroSection.tsx:**
    - Async server component using `getTranslations('Hero')` from 'next-intl/server'
    - Display: name (h1, large bold), title (subtitle text), intro (description text)
    - Social links row with lucide-react icons: Mail (phoonil0927@gmail.com mailto), Github (https://github.com/HoonilP), Globe/ExternalLink (https://velog.io/@hoonilpark)
    - Section id="hero" for nav anchor
    - Responsive: text-3xl on mobile, text-5xl md:, text-6xl lg: for name
    - Use max-w-3xl mx-auto for content width, centered text on mobile, left-aligned on md+
    - Consistent py-20 section padding
    - Social links as icon buttons with hover states using existing design tokens (neutral-600 hover:neutral-900, dark variants)
    - Import icons from 'lucide-react': `Mail`, `Github`, `ExternalLink`

    **AboutSection.tsx:**
    - Async server component using `getTranslations('About')`
    - Section id="about" with py-20 padding
    - Section title (h2) from t('sectionTitle')
    - Two paragraphs from t('paragraph1') and t('paragraph2')
    - Use max-w-3xl mx-auto for readable line length
    - Text styling: text-neutral-600 dark:text-neutral-400 for paragraphs, space-y-6 between paragraphs
    - Section title: text-3xl font-bold md:text-4xl

    **ContactSection.tsx:**
    - Async server component using `getTranslations('Contact')`
    - Section id="contact" with py-20 padding
    - Section title (h2) + description text
    - Grid of contact cards (grid-cols-1 md:grid-cols-2 gap-4):
      - Email: Mail icon, label from t('email'), value "phoonil0927@gmail.com", mailto: link
      - Phone: Phone icon, label from t('phone'), value "010-5557-6835", tel: link
      - GitHub: Github icon, label from t('github'), value "@HoonilP", external link with target="_blank" rel="noopener noreferrer"
      - Velog: ExternalLink icon, label from t('velog'), value "velog.io/@hoonilpark", external link
    - Each contact card: flex items-center gap-3 p-4 with border border-neutral-200 dark:border-neutral-800 rounded-lg hover:border-primary-500 transition-colors (matches Card component border-only style)
    - Use max-w-3xl mx-auto for content width
    - Import icons from 'lucide-react': `Mail`, `Phone`, `Github`, `ExternalLink`

    **All components must:**
    - Be async functions (not 'use client')
    - Use `getTranslations` from 'next-intl/server' (NOT useTranslations)
    - Follow mobile-first responsive design (unprefixed = mobile, md: = tablet+)
    - Use existing design tokens for colors (neutral-*, primary-*, dark: variants)
    - NOT use shadows (border-only aesthetic per design decision)
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors
    - All three files exist and export async default functions
    - No 'use client' directive in any section component
    - All components import from 'next-intl/server', not 'next-intl'
  </verify>
  <done>Three async server section components created with lucide-react icons, bilingual getTranslations, responsive layout, and consistent py-20 spacing</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `npx tsc --noEmit` passes type checking
3. All section components use getTranslations (server-side), not useTranslations
4. Translation files are valid JSON with correct bilingual content
5. lucide-react icons imported in Hero and Contact sections
</verification>

<success_criteria>
- HeroSection displays name, title, intro, and social links with lucide-react icons
- AboutSection displays bilingual self-introduction with overseas experience mention
- ContactSection displays email, phone, GitHub, Velog as a contact card grid
- All content switches correctly between Korean and English
- Components follow server component pattern (async + getTranslations)
- Consistent py-20 spacing and max-w container widths
</success_criteria>

<output>
After completion, create `.planning/phases/03-main-page-sections/03-01-SUMMARY.md`
</output>
