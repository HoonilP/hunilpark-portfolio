---
phase: 08-3d-scenes
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/components/lab2/scene/chapters/IntroScene.tsx
  - src/components/lab2/scene/chapters/Project1Scene.tsx
  - src/components/lab2/scene/chapters/Project2Scene.tsx
  - src/components/lab2/scene/chapters/Project3Scene.tsx
  - src/components/lab2/scene/chapters/Project4Scene.tsx
  - src/components/lab2/scene/chapters/Project5Scene.tsx
  - src/components/lab2/scene/SceneManager.tsx
  - src/components/lab2/Lab2Scene.tsx
autonomous: false
requirements: [SCENE-01, SCENE-02, SCENE-04]

must_haves:
  truths:
    - "IntroScene displays the developer name and title as 3D text in the scene"
    - "Each of the 5 project chapters has visually distinct 3D content with project hero images"
    - "WebP images from /public/projects/{id}/hero.webp render as texture planes in each project scene"
    - "Scrolling through the page reveals each scene at its chapter waypoint"
    - "EmptyScene is removed and replaced by SceneManager + ParticleField in Lab2Scene"
  artifacts:
    - path: "src/components/lab2/scene/chapters/IntroScene.tsx"
      provides: "3D text name/title scene using drei Text"
      exports: ["default"]
    - path: "src/components/lab2/scene/chapters/Project1Scene.tsx"
      provides: "Ministry of Truth project scene with hero texture"
      exports: ["default"]
    - path: "src/components/lab2/scene/chapters/Project2Scene.tsx"
      provides: "DY Microfinance CMS project scene with hero texture"
      exports: ["default"]
    - path: "src/components/lab2/scene/chapters/Project3Scene.tsx"
      provides: "Joshua AI Agent project scene with hero texture"
      exports: ["default"]
    - path: "src/components/lab2/scene/chapters/Project4Scene.tsx"
      provides: "Retail Store Analysis project scene with hero texture"
      exports: ["default"]
    - path: "src/components/lab2/scene/chapters/Project5Scene.tsx"
      provides: "Dino Go project scene with hero texture"
      exports: ["default"]
    - path: "src/components/lab2/Lab2Scene.tsx"
      provides: "Updated Canvas with SceneManager and ParticleField instead of EmptyScene"
      contains: "SceneManager"
  key_links:
    - from: "src/components/lab2/scene/SceneManager.tsx"
      to: "src/components/lab2/scene/chapters/*.tsx"
      via: "Scene components rendered inside visibility-toggled groups"
      pattern: "IntroScene|Project[1-5]Scene"
    - from: "src/components/lab2/Lab2Scene.tsx"
      to: "src/components/lab2/scene/SceneManager.tsx"
      via: "SceneManager mounted inside Canvas Suspense boundary"
      pattern: "SceneManager"
    - from: "src/components/lab2/scene/chapters/Project*Scene.tsx"
      to: "src/components/lab2/scene/shared/TexturePlane.tsx"
      via: "TexturePlane with hero.webp URL from public folder"
      pattern: "TexturePlane"
---

<objective>
Create 6 scene components (IntroScene + 5 ProjectScenes) with 3D text, texture planes, and decorative geometry, then wire them into SceneManager and replace EmptyScene in Lab2Scene.

Purpose: Populate the scroll-driven camera path with actual 3D content so each chapter has visually distinct, meaningful scenes that showcase the portfolio projects.

Output: 6 scene components, updated SceneManager with real scenes, updated Lab2Scene with SceneManager + ParticleField
</objective>

<execution_context>
@/Users/hipark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hipark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-3d-scenes/08-RESEARCH.md
@.planning/phases/08-3d-scenes/08-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 output that this plan depends on -->

From src/components/lab2/config/chapters.ts (Plan 01):
```typescript
export interface Chapter {
  id: string;
  label: string;
  position: THREE.Vector3;
  lookAt: THREE.Vector3;
  projectId?: string;
  translationKey?: string;
}
// Chapter-to-project mapping:
// Chapter 1: projectId='6' (artWar/Ministry of Truth)
// Chapter 2: projectId='2' (dyCms/DY Microfinance CMS)
// Chapter 3: projectId='1' (joshua/Joshua AI Agent)
// Chapter 4: projectId='3' (retailAnalysis/Retail Store Analysis)
// Chapter 5: projectId='5' (dinoGo/Dino Go)
```

From src/components/lab2/scene/SceneManager.tsx (Plan 01):
```typescript
// Renders 6 <group> elements positioned at scene focal centers:
// Group 0: position=[0, 0, 0]        — IntroScene
// Group 1: position=[1, 0, -5]       — Project1Scene (artWar)
// Group 2: position=[-1, 0, -11]     — Project2Scene (dyCms)
// Group 3: position=[0, 0, -17]      — Project3Scene (joshua)
// Group 4: position=[0, 0, -23]      — Project4Scene (retailAnalysis)
// Group 5: position=[0, 0, -29]      — Project5Scene (dinoGo)
// Currently has placeholder meshes — replace with real scene components
```

From src/components/lab2/scene/shared/TexturePlane.tsx (Plan 01):
```typescript
interface TexturePlaneProps {
  url: string;
  width?: number;
  height?: number;
  position?: [number, number, number];
  rotation?: [number, number, number];
}
export default function TexturePlane(props: TexturePlaneProps): JSX.Element;
```

Available project images in /public/projects/:
- /projects/1/hero.webp, /projects/1/architecture.webp, /projects/1/thumbnail.webp
- /projects/2/hero.webp, /projects/2/thumbnail.webp
- /projects/3/hero.webp, /projects/3/feature1.webp, /projects/3/feature2.webp, /projects/3/thumbnail.webp
- /projects/4/hero.webp, /projects/4/thumbnail.webp
- /projects/5/hero.webp, /projects/5/thumbnail.webp
- /projects/6/hero.webp, /projects/6/thumbnail.webp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IntroScene and 5 ProjectScene components</name>
  <files>
    src/components/lab2/scene/chapters/IntroScene.tsx
    src/components/lab2/scene/chapters/Project1Scene.tsx
    src/components/lab2/scene/chapters/Project2Scene.tsx
    src/components/lab2/scene/chapters/Project3Scene.tsx
    src/components/lab2/scene/chapters/Project4Scene.tsx
    src/components/lab2/scene/chapters/Project5Scene.tsx
  </files>
  <action>
Create the `src/components/lab2/scene/chapters/` directory and all 6 scene components. All components use `'use client'` directive.

**IMPORTANT:** All positions in these scene components are RELATIVE to the parent group's position in SceneManager. The SceneManager group already positions each scene at the focal center, so scene-internal positions should be relative offsets from (0, 0, 0).

**IntroScene.tsx (SCENE-01):**
- Import `Text` from `@react-three/drei`
- Render the developer name "박훈일" using `<Text>`:
  - position=[0, 0.8, 0], fontSize=0.6, color="#ffffff"
  - font="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/woff/Pretendard-Bold.woff"
  - anchorX="center", anchorY="middle"
- Render the title "Frontend Developer" below:
  - position=[0, 0, 0], fontSize=0.18, color="#888888"
  - Same font URL but Regular weight: Pretendard-Regular.woff
  - anchorX="center", anchorY="middle"
- Add a subtle decorative element: a thin wireframe `<mesh>` ring (torusGeometry) around the text:
  - `<torusGeometry args={[1.8, 0.005, 16, 64]} />`
  - `<meshBasicMaterial color="#333333" wireframe />`
  - position=[0, 0.4, -0.5], rotation=[Math.PI/6, 0, 0]
- Add ambient light: `<ambientLight intensity={0.3} />`

**Project1Scene.tsx (artWar / Ministry of Truth, projectId=6):**
- Import TexturePlane from `../shared/TexturePlane`
- Render hero image: `<TexturePlane url="/projects/6/hero.webp" width={3.5} height={2.2} position={[0.5, 0.8, 0]} rotation={[0, -0.15, 0]} />`
- Add decorative geometry: a few floating `<mesh>` boxes with wireframe material, scattered around the image at varying small sizes (0.2-0.4), rotated slightly, using color "#444444"
- Add a subtle point light: `<pointLight position={[2, 2, 2]} intensity={0.3} color="#ff6644" />`
- Call `useTexture.preload('/projects/6/hero.webp')` at module level (outside component)

**Project2Scene.tsx (dyCms / DY Microfinance CMS, projectId=2):**
- Import TexturePlane
- Render hero image: `<TexturePlane url="/projects/2/hero.webp" width={3.5} height={2.2} position={[-0.5, 0.8, 0]} rotation={[0, 0.12, 0]} />`
- Decorative geometry: 2-3 flat `<mesh>` planes stacked with slight offset (like floating dashboard cards), using `<meshBasicMaterial color="#1a1a2e" transparent opacity={0.3} />`
- Point light: `<pointLight position={[-2, 2, 1]} intensity={0.3} color="#4488ff" />`
- Preload: `useTexture.preload('/projects/2/hero.webp')`

**Project3Scene.tsx (joshua / Joshua AI Agent, projectId=1):**
- Import TexturePlane
- Render hero image: `<TexturePlane url="/projects/1/hero.webp" width={3.5} height={2.2} position={[0, 0.8, 0.3]} rotation={[0, 0, 0]} />`
- If architecture image exists, add a smaller secondary plane: `<TexturePlane url="/projects/1/architecture.webp" width={2} height={1.3} position={[2.2, 0.5, 0.8]} rotation={[0, -0.3, 0.02]} />`
- Decorative: a floating icosahedron wireframe (representing AI): `<mesh position={[-2, 1.2, 0.5]}><icosahedronGeometry args={[0.4, 0]} /><meshBasicMaterial color="#44ff88" wireframe /></mesh>`
- Point light: `<pointLight position={[0, 3, 2]} intensity={0.3} color="#44cc66" />`
- Preload both: `useTexture.preload('/projects/1/hero.webp')` and `useTexture.preload('/projects/1/architecture.webp')`

**Project4Scene.tsx (retailAnalysis / Retail Store Analysis, projectId=3):**
- Import TexturePlane
- Render hero image: `<TexturePlane url="/projects/3/hero.webp" width={3.5} height={2.2} position={[0.3, 0.8, 0]} rotation={[0, -0.1, 0]} />`
- Decorative: 2-3 small spheres (representing data points / tracking dots) at scattered positions: `<mesh position={[x, y, z]}><sphereGeometry args={[0.08, 16, 16]} /><meshBasicMaterial color="#ff8844" /></mesh>`
- Point light: `<pointLight position={[1, 2, 2]} intensity={0.3} color="#ff8844" />`
- Preload: `useTexture.preload('/projects/3/hero.webp')`

**Project5Scene.tsx (dinoGo / Dino Go, projectId=5):**
- Import TexturePlane
- Render hero image: `<TexturePlane url="/projects/5/hero.webp" width={3.5} height={2.2} position={[-0.3, 0.8, 0]} rotation={[0, 0.1, 0]} />`
- Decorative: a dodecahedron wireframe (representing blockchain / gem-like): `<mesh position={[2, 0.6, 0.5]}><dodecahedronGeometry args={[0.35, 0]} /><meshBasicMaterial color="#8844ff" wireframe /></mesh>`
- Point light: `<pointLight position={[-1, 2, 2]} intensity={0.3} color="#8866ff" />`
- Preload: `useTexture.preload('/projects/5/hero.webp')`

**All ProjectScenes must:**
- Use `'use client'` directive
- Import TexturePlane from the shared component
- Call `useTexture.preload()` at module level for each URL used
- Have no `useState` for visual state — all static geometry
- Export default function component
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>IntroScene renders 3D text name/title with decorative ring, 5 ProjectScenes each render a TexturePlane with project hero image plus unique decorative geometry — all compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire scenes into SceneManager and replace EmptyScene in Lab2Scene</name>
  <files>
    src/components/lab2/scene/SceneManager.tsx
    src/components/lab2/Lab2Scene.tsx
  </files>
  <action>
**Update SceneManager.tsx:**
- Remove placeholder meshes from each group
- Import all 6 scene components:
  ```
  import IntroScene from './chapters/IntroScene';
  import Project1Scene from './chapters/Project1Scene';
  import Project2Scene from './chapters/Project2Scene';
  import Project3Scene from './chapters/Project3Scene';
  import Project4Scene from './chapters/Project4Scene';
  import Project5Scene from './chapters/Project5Scene';
  ```
- Create a `SCENES` array: `[IntroScene, Project1Scene, Project2Scene, Project3Scene, Project4Scene, Project5Scene]`
- In the render, map over SCENES and render each inside its positioned group:
  ```tsx
  {SCENES.map((SceneComponent, i) => (
    <group key={i} ref={(el) => { groupRefs.current[i] = el; }} position={SCENE_POSITIONS[i]}>
      <SceneComponent />
    </group>
  ))}
  ```
- Keep the SCENE_POSITIONS array (the focal-center positions from Plan 01) as a module-level constant

**Update Lab2Scene.tsx:**
- Remove `import EmptyScene from './EmptyScene'`
- Add `import SceneManager from './scene/SceneManager'`
- Add `import ParticleField from './scene/ParticleField'`
- Inside the Canvas `<Suspense>` boundary, replace `<EmptyScene />` with `<SceneManager />` and `<ParticleField />`
- Keep `<CameraRig />` and `<Preload all />` in place
- Add global ambient light: `<ambientLight intensity={0.15} />` (replaces the one from EmptyScene)
- Remove the gridHelper that was in EmptyScene (it was a dev reference)
- Do NOT delete EmptyScene.tsx file — it may be referenced elsewhere. Just stop importing it in Lab2Scene.

The final Lab2Scene Canvas contents should be:
```tsx
<Canvas camera={{fov: 50, position: [0, 2, 5], near: 0.01, far: 100}} gl={{antialias: true, alpha: false}} dpr={[1, 1.5]}>
  <color attach="background" args={['#080808']} />
  <Suspense fallback={null}>
    <ambientLight intensity={0.15} />
    <CameraRig />
    <SceneManager />
    <ParticleField />
    <Preload all />
  </Suspense>
</Canvas>
```
  </action>
  <verify>
    <automated>cd /Users/hipark/dev/portfolio && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>SceneManager renders all 6 real scene components inside visibility-toggled groups, Lab2Scene uses SceneManager + ParticleField instead of EmptyScene, TypeScript compiles with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete 3D scene system: IntroScene with 3D text name/title, 5 ProjectScenes with hero image texture planes and decorative geometry, particle field background, and visibility-toggle scene switching driven by scroll</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/ko/lab2
    2. After loading screen, you should see the IntroScene with "박훈일" and "Frontend Developer" as 3D text
    3. Scroll down slowly — the camera should move through 6 chapters, each showing a different scene:
       - Chapter 0 (Intro): Name/title text with decorative ring
       - Chapter 1: Ministry of Truth hero image on a 3D plane
       - Chapter 2: DY Microfinance CMS hero image
       - Chapter 3: Joshua AI Agent hero image (plus architecture.webp as secondary plane)
       - Chapter 4: Retail Store Analysis hero image
       - Chapter 5: Dino Go hero image
    4. Each project scene should have a unique accent color from its point light
    5. Ambient particles should be visible throughout, slowly rotating
    6. Verify no visible stutter at chapter transitions (visibility toggle, no remounting)
    7. Check browser console for any Three.js warnings or errors
  </how-to-verify>
  <resume-signal>Type "approved" if scenes render correctly, or describe any issues (wrong positions, missing textures, visual glitches)</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Lab2Scene.tsx imports SceneManager and ParticleField (not EmptyScene)
3. All 6 scene components exist in `src/components/lab2/scene/chapters/`
4. Each ProjectScene calls `useTexture.preload()` at module level
5. IntroScene uses drei `<Text>` component with Korean text
6. SceneManager imports and renders all 6 real scene components (no more placeholders)
7. No conditional rendering pattern (`{idx === X && <Scene />}`) anywhere — only visibility toggle
</verification>

<success_criteria>
- IntroScene renders "박훈일" and "Frontend Developer" in 3D text (SCENE-01)
- 5 project scenes each have hero.webp texture planes with unique decorative geometry (SCENE-02, SCENE-04)
- All scenes are wired into SceneManager's visibility-toggle groups
- Lab2Scene uses SceneManager + ParticleField instead of EmptyScene
- Scrolling reveals each scene at its chapter waypoint
- Human verification confirms visual correctness in browser
</success_criteria>

<output>
After completion, create `.planning/phases/08-3d-scenes/08-02-SUMMARY.md`
</output>
